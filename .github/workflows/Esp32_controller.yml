name: Build Single Factory Binary Bluepad32 for ESP32

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    # Menggunakan Container SDK Resmi dari Espressif (ESP-IDF v5.3)
    container:
      image: espressif/idf:v5.3

    steps:
      - name: Clone Official Bluepad32 Repository (Recursive)
        run: |
          git clone --recursive https://github.com/ricardoquesada/bluepad32.git official_bluepad32

      - name: Integrasi BTStack ke Source Code Bluepad32
        run: |
          cd official_bluepad32/external/btstack/port/esp32
          # Menjalankan perintah integrasi persis sesuai instruksi resmi Bluepad32
          IDF_PATH=../../../../src ./integrate_btstack.py

      - name: Compile Firmware ESP32
        run: |
          # Mengaktifkan environment path ESP-IDF bawaan Docker container
          . /opt/esp/idf/export.sh
          
          cd official_bluepad32/examples/esp32
          idf.py set-target esp32
          idf.py build

      - name: Install esptool & Merge Binary Menjadi 1 File (.bin)
        run: |
          # Install esptool ke dalam Python environment container
          python -m pip install esptool
          
          cd official_bluepad32/examples/esp32/build
          
          # Menggabungkan Bootloader (0x1000), Partition Table (0x8000), dan Application (0x10000)
          esptool.py --chip esp32 merge_bin \
            -o merged-bluepad32-esp32.bin \
            --flash_mode dio \
            --flash_freq 40m \
            --flash_size 4MB \
            0x1000 bootloader/bootloader.bin \
            0x8000 partition_table/partition-table.bin \
            0x10000 bluepad32-esp32.bin

          mkdir -p $GITHUB_WORKSPACE/output_bin
          cp merged-bluepad32-esp32.bin $GITHUB_WORKSPACE/output_bin/

      - name: Upload Single BIN File sebagai GitHub Artifact
        uses: actions/upload-artifact@v4
        with:
          name: bluepad32-esp32-single-factory-bin
          path: output_bin/merged-bluepad32-esp32.bin
